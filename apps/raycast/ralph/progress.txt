# Ralph Progress Log

## Codebase Patterns (READ THIS FIRST!)
<!-- Add patterns discovered during implementation. Future iterations will read this first. -->
- Example: "Always use IF NOT EXISTS for migrations"
- Example: "React refs must be typed as useRef<Type | null>(null)"
- Example: "API responses follow {data, error, meta} structure"

## Key Files
<!-- Document important files and their purposes -->
- src/main.ts - Application entry point
- src/config.ts - Configuration management

## Gotchas & Warnings
<!-- Document things that caused issues -->
- Example: "Don't modify X without updating Y"
- Example: "Tests require DATABASE_URL to be set"

## Session Log
<!-- Each iteration appends learnings below this line -->
---

# @raggle/raggle-local

Local project discovery and expansion logic for Raggle.

## Install

```sh
npm install github:raggle-ai/local
```

## Development

```sh
npm install
npm run typecheck
npm run lint
npm run build
```

The package publishes compiled files from `dist` and TypeScript source from `src`.
